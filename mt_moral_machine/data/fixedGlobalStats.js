/**
 * Canonical “global pulse” numbers for the fixed-order build.
 * The stats API returns these every time so each run sees the same reference panel.
 */
export const FIXED_GLOBAL_STATS = {
  globalSwervePercent: 40,
  globalStayPercent: 60,
  playersAgreePercent: 67,
  avgReactionMs: 850,
};
